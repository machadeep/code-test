name: Example Workflow

on:
  push:
    branches:
      - main

jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - name: Use the secret token
        run: |
          echo "My token is ${{ secrets.GITHUBTOKEN }}"

